# Интерактивная карта России - Руководство

## 🗺️ Обзор

Интерактивная SVG карта России с детальной разбивкой Дальневосточного федерального округа по субъектам.

Источник вдохновения: [Atuin.ru - Карта субъектов РФ в SVG](https://atuin.ru/blog/karta-subektov-rf-v-svg/)

## ✨ Функциональность

### 1. Клик по региону
При клике на любой регион ДФО открывается модальное окно с информацией:
- Название региона
- Описание и особенности
- Список высших учебных заведений
- Ссылка на официальный сайт региона

### 2. Hover-эффекты
При наведении курсора на регион:
- Регион меняет цвет (становится светлее)
- Появляется синяя точка-маркер
- Показывается название региона

### 3. Модальное окно
Красиво оформленное окно с:
- Плавной анимацией появления
- Затемнением фона
- Кнопкой закрытия (×)
- Адаптивным дизайном

## 🎯 Регионы ДФО

| Код | Регион | Университет | Цвет |
|-----|--------|-------------|------|
| RU-SA | Республика Саха (Якутия) | СВФУ | Светло-синий (#3a6da0) |
| RU-KHA | Хабаровский край | ТОГУ | Синий (#2B5A8E) |
| **RU-PRI** | **Приморский край** | **ДВФУ** | **Темно-синий (#1e4976)** |
| RU-AMU | Амурская область | АмГУ | Синий (#2B5A8E) |
| RU-KAM | Камчатский край | КамГУ | Синий (#2B5A8E) |
| RU-SAK | Сахалинская область | СахГУ | Синий (#2B5A8E) |
| RU-MAG | Магаданская область | СВГУ | Синий (#2B5A8E) |
| RU-CHU | Чукотский АО | Филиалы | Синий (#2B5A8E) |
| RU-YEV | Еврейская АО | ПГУ | Синий (#2B5A8E) |

> **Примечание:** Приморский край выделен темно-синим цветом как расположение ДВФУ

## 📝 Использование

### Добавление нового региона

1. Добавьте данные в `RussiaMapData.js`:

```javascript
export const dfoRegions = {
  'RU-XXX': {
    name: 'Название региона',
    info: 'Описание региона',
    universities: ['Университет 1', 'Университет 2'],
    website: 'https://region.gov.ru',
    highlight: false // true для особого выделения
  }
};
```

2. Добавьте path в SVG карту в `Header.js`:

```jsx
<path 
  d="M ... Z" 
  fill="#2B5A8E" 
  stroke="#1e3a52" 
  strokeWidth="1.2" 
  opacity="0.95"
  className="dfo-region" 
  data-code="RU-XXX"
  onClick={() => handleRegionClick('RU-XXX')}
  onMouseEnter={(e) => handleRegionHover(e, 'Название региона')}
  onMouseLeave={handleRegionLeave}
/>
```

### Изменение стилей модального окна

Отредактируйте `Header.css`:

```css
.region-modal {
  background: white;
  border-radius: 12px;
  padding: 40px;
  max-width: 600px;
  /* Ваши изменения */
}
```

## 🎨 Стилизация

### Цвета карты

```css
/* Регионы ДФО */
.dfo-region {
  fill: #2B5A8E; /* Базовый синий */
}

.dfo-region:hover {
  fill: #4a7fb0; /* Светлеет при hover */
}

/* Особое выделение (Приморский край) */
.dfo-highlight {
  fill: #1e4976; /* Темно-синий */
}
```

### Анимации

```css
/* Появление overlay */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

/* Выезд модального окна */
@keyframes slideUp {
  from {
    transform: translateY(50px);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}
```

## 🔧 Техническая реализация

### React Hooks

```javascript
// Состояние выбранного региона
const [selectedRegion, setSelectedRegion] = useState(null);

// Данные для tooltip
const [tooltipData, setTooltipData] = useState({ 
  show: false, 
  name: '', 
  x: 0, 
  y: 0 
});
```

### Обработчики событий

```javascript
// Клик по региону - открыть модалку
const handleRegionClick = (regionCode) => {
  const region = dfoRegions[regionCode];
  if (region) {
    setSelectedRegion({ code: regionCode, ...region });
  }
};

// Наведение - показать tooltip
const handleRegionHover = (e, regionName) => {
  const svgRect = e.currentTarget.closest('svg').getBoundingClientRect();
  const pathRect = e.currentTarget.getBoundingClientRect();
  setTooltipData({
    show: true,
    name: regionName,
    x: pathRect.left - svgRect.left + pathRect.width / 2,
    y: pathRect.top - svgRect.top
  });
};

// Уход курсора - скрыть tooltip
const handleRegionLeave = () => {
  setTooltipData({ show: false, name: '', x: 0, y: 0 });
};
```

## 📱 Адаптивность

### Desktop (> 1200px)
- Полный размер карты
- Модалка по центру экрана

### Tablet (768px - 1200px)
- Уменьшенная карта
- Адаптивная модалка

### Mobile (< 768px)
- Карта на всю ширину
- Модалка 95% ширины
- Уменьшенные отступы
- Увеличенная кнопка закрытия

## 🐛 Отладка

### Проверка загрузки данных

```javascript
console.log(dfoRegions); // Все регионы
console.log(dfoRegions['RU-PRI']); // Конкретный регион
```

### Проверка обработчиков

```javascript
document.querySelectorAll('.dfo-region').forEach(region => {
  console.log(region.getAttribute('data-code'));
});
```

## 📚 Ссылки

- **Оригинальная статья**: [Atuin.ru - Карта субъектов РФ в SVG](https://atuin.ru/blog/karta-subektov-rf-v-svg/)
- **SVG спецификация**: [MDN Web Docs - SVG](https://developer.mozilla.org/en-US/docs/Web/SVG)
- **React hooks**: [React Documentation](https://react.dev/reference/react)

## 💡 Советы

1. **Координаты SVG** - используйте редактор вроде Inkscape для создания path
2. **Тестирование** - проверяйте на разных разрешениях экрана
3. **Производительность** - SVG оптимизирован, но следите за количеством элементов
4. **Accessibility** - добавляйте aria-labels для screen readers

## 🚀 Будущие улучшения

- [ ] Добавить анимацию при загрузке карты
- [ ] Реализовать zoom функцию
- [ ] Добавить клавиатурную навигацию (ESC для закрытия)
- [ ] Статистику по регионам (количество вузов, студентов)
- [ ] Легенду карты с расшифровкой цветов
- [ ] Мобильный список регионов под картой

---

**Версия:** 1.2.0  
**Дата:** Ноябрь 2024  
**Автор:** DVFU Rectorat Team

